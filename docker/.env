# 是否干跑模式（true=只测试不实际执行，false=正常执行）
DRYRUN=false
# 你的主域名（替换yourdomain.com）
DOMAIN=yourdomain.com
# API服务的子域名前缀（与nginx配置中的gptj.api对应）
SUBDOMAIN=gptj.api
# Nginx配置文件的相对路径（通常保持默认即可）
NGINX_PATH=./nginx_proxyvm.conf
# SSL证书存放路径（替换为你的证书实际路径）
CERTS_PATH=/path/to/certs/
# DNS API密钥文件路径（用于自动申请证书）
DNS_KEYS=/path/to/keys/
# 模型文件目录路径（替换为你的模型实际路径）
MODEL_DIR=/your/path/to/model/step_383500
# 你的Google Cloud TPU名称
TPU_NAME=YOUR_TPU_NAME